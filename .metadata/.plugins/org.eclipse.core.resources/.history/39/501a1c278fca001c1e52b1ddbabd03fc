
public class Test {
	public static void main(String[] args) {
		int max = 400;
		for (int a = 1; a < max; a++) {
			for (int b = 1; b < max; b++) {
				for (int c = 1; c < max; c++) {
					if (a*a*(a-3*b-3*c) + b*b*(b-3*a-3*c) + c*c*(c-3*a-3*b) - 5*a*b*c == 0) {
						System.out.println("(a,b,c) >>> (" + a + "," + b + "," + c + ")");
						break;
					}
				}
			}
		}
	}
}
